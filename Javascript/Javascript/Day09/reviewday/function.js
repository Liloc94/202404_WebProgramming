// [동]
// function make(x) {
//   return x + 10;
// }

// const div = document.createElement("div");
// div.appendChild(".box");
// div.innerText = "내용"01

// 1 -> 101, 2 -> 102, 3 -> 103;
// f(k,g(x)) [ 합성 함수]
addEventListener("click", () => {});
// map(()=>{})
function x100(x) {
  return x + 100;
}

x100(1); // number type
x100("123"); // string type
x100(true); // boolean type
x100([1, 2, 3, 4, 5]); // Array type
x100(() => {}); //

// 기초적인 기능들은 다 숙지하고 있는데 그 활용을 제대로 못하고 있는 것 같다.
// 문제해결을 위한 로직을 떠올리거나 만드는 것이 어렵다.
// 코드 실행 순서나 흐름에 대한 직관적인 파악이 어렵다.

// ex) 1 ~ 10 까지 수가 들어있는 배열에서 랜덤한 횟수로 수를 입력받고
// 입력받은 수에 해당하는 수를 배열에서 삭제한다.
// 지워진 수를 통해 몇회 입력받았는지 구하는 함수 작성

// forEach 활용이나 object 타입을 활용할 창의성? 이 부족한것 같다
